import pandas as pd


def create_dataframes():

    df_eips = pd.DataFrame()
    df_oldami = pd.DataFrame()
    df_ebssnaps = pd.DataFrame()
    df_vol = pd.DataFrame()
    df_unami = pd.DataFrame()
    df_rdssnaps = pd.DataFrame()

    return df_eips, df_oldami, df_ebssnaps, df_vol, df_unami, df_rdssnaps
